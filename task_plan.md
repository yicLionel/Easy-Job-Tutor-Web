# Task Plan: Easy Job Tutor 网页主产品

## Goal

依据已批准规格，完成可本地运行、可验证的 AI 产品经理岗位差距闭环网页 MVP，包括版本化语料、15 秒真实快速结果、七天任务、复测、隐私删除与自动化测试。

## Phases

- [x] Phase 1: 完成产品设计规格并取得批准
- [x] Phase 2: 编写核心网页与离线语料两份实施计划
- [ ] Phase 3: 以 TDD 实现领域、持久化、分析与隐私能力
- [ ] Phase 4: 实现生产级响应式界面与完整用户路径
- [ ] Phase 5: 实现离线语料管线和首个审核快照
- [ ] Phase 6: 完成单元、集成、E2E、性能与视觉验收
- [ ] Phase 7: 对照规格逐项完成审计并交付

## Key Questions

1. 如何让确定性分析在模型超时时仍满足 15 秒首个真实结果？
2. 如何保证未确认事实、过期异步结果和来源缺失不会污染当前结论？
3. 如何把语料采集与用户运行链路完全隔离，又保持历史结果可解释？
4. 如何在不建设复杂账号的情况下实现安全恢复和彻底删除？
5. 哪些验证属于工程证据，哪些仍需要真实 Beta 用户？

## Decisions Made

- 网页核心与离线语料分别制定实施计划，共享版本化快照合同。
- 用户已授权由 Codex 自主完成普通技术、视觉和测试决策。
- 采用测试驱动与阶段性提交；不以测试全绿替代内容和产品价值验证。
- 依赖安装使用项目专用 npm 缓存，不修复或改写用户的全局 npm 缓存权限。
- 实施拆为核心网页 18 个可验收任务与语料管线 6 个可验收任务；采用 Subagent-Driven 执行并在任务间审查。
- 未完成人工内容审核时只能生成 `editorial_candidate`，运行时继续使用显式标注的 demo 快照。

## Errors Encountered

- `npm view` 使用默认缓存时因 `/Users/yyc/.npm/_cacache` 含 root-owned 文件而返回 `EPERM`。解决方案：后续使用项目专用 `npm_config_cache` 路径，不运行 `sudo chown`。
- 实施计划首次暂存检查发现命令行与期望行使用 Markdown 双空格换行，`git diff --check` 拒绝提交；已批量移除纯格式尾空格，提交在修复前未发生。

## Status

**Currently in Phase 3** - 两份实施计划已完成自审，准备按 TDD 和任务级审查开始实现。
